import 'package:flutter/material.dart';

class OurContact extends StatelessWidget {
  const OurContact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}